# Copy this file to examples/ssail/.env (already gitignored via the repo's top-level
# `.env` pattern) and fill in real values. setup_env.sh, start_train.sh, and the
# gpu/neuron launchers all source examples/ssail/.env automatically if it exists —
# nothing here is ever meant to be committed.

MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=
